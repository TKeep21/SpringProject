spring:
  application:
    name: report-service

server:
  port: ${REPORT_SERVICE_PORT:8083}

finance-service:
  base-url: ${FINANCE_SERVICE_BASE_URL:http://localhost:8082}

jwt:
  secret: ${JWT_SECRET:change-me-change-me-change-me-change-me-1234567890}

management:
  endpoints:
    web:
      exposure:
        include: health
